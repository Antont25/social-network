import React from 'react';
import {Paper} from "@material-ui/core";

const Footer = () => {
    return (
        <Paper elevation={3} style={{background: 'rgb(65, 72, 79)', height: '100px', width: '100%'}}>

        </Paper>
    );
};

export default Footer;